{
  "name": "pulse",
  "private": true,
  "version": "1.0.0",
  "description": "Pulse \u2014 activity & churn-risk tracking for HighLevel sub-accounts",
  "workspaces": [
    "packages/*",
    "apps/collector",
    "apps/worker",
    "apps/web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build -w @pulse/core && npm run build -w @pulse/db && npm run build -w @pulse/collector && npm run build -w @pulse/worker && npm run build:script",
    "typecheck": "tsc -b packages/core packages/db apps/collector apps/worker",
    "test": "npm run test -w @pulse/core && npm run build:script",
    "dev:collector": "npm run dev -w @pulse/collector",
    "dev:worker": "npm run dev -w @pulse/worker",
    "dev:web": "npm run dev -w apps/web",
    "build:script": "esbuild tracking/pulse.js --minify --target=es2015 --outfile=tracking/pulse.min.js && node scripts/check-script-size.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
